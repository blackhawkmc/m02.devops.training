version: v1.0
name: Demo CI pipeline
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Build
    task:
      jobs:
        - name: Make app
          commands:
            - checkout
            - make app
            - artifacts push workflow app
            - echo hello
